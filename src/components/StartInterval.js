import React from 'react'

const StartInterval = (props) => {
    
    return(
        <div>
            <button onClick={props.start} >Start Interval Game</button>
        </div> 
    )
}

export default StartInterval